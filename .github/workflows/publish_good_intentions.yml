name: Publish good_intentions

on:
  workflow_dispatch:
    inputs:
      bump:
        description: "Version bump type"
        required: true
        default: patch
        type: choice
        options:
          - patch
          - minor
          - major
  push:
    tags:
      - "good_intentions-v*"

permissions:
  contents: write
  id-token: write

jobs:
  version:
    name: Create Version Tag
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_TOKEN }}

      - name: Determine next version and push tag
        shell: bash
        run: |
          set -euo pipefail
          latest=$(git tag -l 'good_intentions-v*' --sort=-v:refname | head -n1)
          if [ -z "$latest" ]; then
            latest="good_intentions-v0.0.0"
          fi

          version="${latest#good_intentions-v}"
          IFS='.' read -r major minor patch <<< "$version"

          case "${{ inputs.bump }}" in
            major) major=$((major + 1)); minor=0; patch=0 ;;
            minor) minor=$((minor + 1)); patch=0 ;;
            patch) patch=$((patch + 1)) ;;
          esac

          tag="good_intentions-v${major}.${minor}.${patch}"
          echo "Bumping ${latest} -> ${tag}"
          git tag "$tag"
          git push origin "$tag"

  publish:
    name: Publish to pub.dev
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Dart
        uses: dart-lang/setup-dart@v1
        with:
          sdk: stable

      - name: Set version from tag
        run: |
          version="${GITHUB_REF_NAME#good_intentions-v}"
          sed -i "s/^version: .*/version: ${version}/" good_intentions/pubspec.yaml

      - name: Copy root README into package
        run: |
          cp README.md good_intentions/README.md
          sed -i 's|good_intentions/coverage_badge.svg|coverage_badge.svg|g' good_intentions/README.md

      - name: Install dependencies
        working-directory: good_intentions
        run: dart pub get

      - name: Publish
        working-directory: good_intentions
        run: dart pub publish --force

  release:
    name: GitHub Release
    runs-on: ubuntu-latest
    needs: publish
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${{ github.ref_name }}" --generate-notes
